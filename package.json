{
  "name": "stickmancommando",
  "version": "0.2.0",
  "description": "Stickman Commando — top-down browser shooter",
  "type": "commonjs",
  "scripts": {
    "check": "node scripts/check.js",
    "lint": "node scripts/check.js",
    "test": "node scripts/check.js",
    "favicon": "node scripts/make-favicon.js",
    "dev": "npx --yes vite --host --port 5173",
    "build": "npx --yes vite build",
    "preview": "npx --yes vite preview"
  },
  "devDependencies": {},
  "license": "MIT"
}
